-- Burgergrill Restaurant Website - Core Business Schema  
-- Built on top of official Supabase schema
--
-- This schema loads only the core requirements for a restaurant website.
-- Clean, simple user management and file storage for menu/content.

-- =============================================================================
-- CORE MODULES (Required for all business models)
-- =============================================================================

\i /docker-entrypoint-initdb.d/zzz-core-schema.sql
